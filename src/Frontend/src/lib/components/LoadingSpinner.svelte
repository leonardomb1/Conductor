<script lang="ts">
  import { cn } from '$lib/utils'
  
  let { size = 'md' } = $props();
  
  const sizes = {
    sm: 'h-4 w-4',
    md: 'h-6 w-6', 
    lg: 'h-8 w-8'
  }
</script>

<div
  class={cn(
    'animate-spin rounded-full border-2 border-current border-t-transparent',
    sizes[size]
  )}
></div>
